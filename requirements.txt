# Librerías básicas para procesamiento de datos
gpxpy>=1.4.0
tqdm>=4.60.0
numpy>=1.21.0
pandas>=1.3.0

# Librerías para machine learning y redes neuronales
# Nota: TensorFlow 2.15.x es la última versión estable con soporte completo para Python 3.12
tensorflow>=2.15.0
scikit-learn>=1.0.0

# Librerías para visualización y gráficos
matplotlib>=3.5.0

# Nota: Las siguientes librerías son módulos estándar de Python y no requieren instalación:
# - json (módulo estándar)
# - os (módulo estándar)  
# - time (módulo estándar)
# - pathlib (módulo estándar desde Python 3.4+)
# - datetime (módulo estándar)
# - xml.etree.ElementTree (módulo estándar)